// src/start/app.ts
var isStart = false;
(async () => {
  if (isStart) return;
  isStart = true;
  const index = import.meta.url.includes(".ts") ? "../index.ts" : "../index.mjs";
  const { start } = await import(index);
  start();
})();
